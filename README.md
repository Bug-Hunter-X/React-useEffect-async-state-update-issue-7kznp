# React useEffect Async State Update Issue

This example demonstrates a common issue in React when working with asynchronous operations inside `useEffect` hooks and updating state in an asynchronous function.  The component renders with the initial state and never updates after data is fetched.